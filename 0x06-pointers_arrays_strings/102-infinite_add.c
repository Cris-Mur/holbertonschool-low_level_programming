#include "holberton.h"

/**
 * infinite_add - add two numbers
 * @n1: pointer input
 * @n2:
 * @r:
 * @size_r:
 * Return: r (success) or 0 (error)
 */

char *infinite_add(char *n1, char *n2, char *r, int size_r)
{
	size_r++;
	return (r);
}
