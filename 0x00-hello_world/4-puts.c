#include <stdio.h>

/**
 * main - entry point
 *
 * Return: always 0 (Success)
 */
int main(void)
{
	char phrase[] = "Programming is like building a multilingual puzzle";

	puts(phrase);
	return (0);
}
